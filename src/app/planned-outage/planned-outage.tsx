import { Typography } from '@mui/material';

/* eslint-disable-next-line */
export interface PlannedOutageProps {}

export function PlannedOutage(props: PlannedOutageProps) {
  return (
    <div>
      <Typography>Welcome to PlannedOutage!</Typography>
    </div>
  );
}

export default PlannedOutage;
